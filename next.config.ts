// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  output: 'export',
  // other settings if needed
};

export default {
  output: 'export',
  basePath: '/Vortix'
};
